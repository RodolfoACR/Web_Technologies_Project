<h1>Hashtags</h1>

<ul>
  <% @hashtags.each do |hashtag| %>
    <li>
      #<%= hashtag.tag %> - 
      <%= link_to "View", hashtag_path(hashtag), class: "btn btn-sm btn-outline-primary" %>
    </li>
  <% end %>

</ul>
